import styled from "styled-components";
import { Nav, Navbar } from "react-bootstrap";

export const NavbarBrandGold = styled(Navbar.Brand)`
  color: gold;
`;

export const CustomNav = styled(Nav)`
  max-height: 100px;
`;
